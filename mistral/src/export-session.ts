// SCAFFOLD — exportação de sessão (mistral_session.json) para deploy headless/Docker (não implementado).
// Referência: glm/src/export-session.ts.
console.error('Exportação de sessão do proxy Mistral ainda não implementada — veja mistral/PLAN.md.')
process.exit(1)

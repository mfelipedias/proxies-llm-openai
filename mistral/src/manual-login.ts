// SCAFFOLD — login manual (navegador visível) do proxy Mistral (não implementado).
// Referência: glm/src/manual-login.ts (abre o navegador, usuário resolve captcha/OAuth, detecta sessão e salva perfil).
console.error('Login manual do proxy Mistral ainda não implementado — veja mistral/PLAN.md.')
process.exit(1)

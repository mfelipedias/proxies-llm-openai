// SCAFFOLD — exportação de sessão (kimi_session.json) para deploy headless/Docker (não implementado).
// Referência: glm/src/export-session.ts.
console.error('Exportação de sessão do proxy Kimi ainda não implementada — veja kimi/PLAN.md.')
process.exit(1)

// SCAFFOLD — login manual (navegador visível) do proxy Kimi (não implementado).
// Referência: glm/src/manual-login.ts (abre o navegador, usuário resolve captcha, detecta sessão e salva perfil).
console.error('Login manual do proxy Kimi ainda não implementado — veja kimi/PLAN.md.')
process.exit(1)
